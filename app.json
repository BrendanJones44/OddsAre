{
  "environments": {
    "test": {
      "scripts": {
        "test-setup": "bundle",
        "test-setup": "rake db:migrate",
        "test": "rubocop"
      }
    }
  },
  "env": {
    "RACK_ENV": "test"
  }
}
