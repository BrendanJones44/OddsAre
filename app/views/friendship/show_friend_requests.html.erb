<% @friend_requests.each do |friend_request| %>
	<ul>
		<li>
			Request From:
			<%= friend_request.to_s %>
			<%= button_to "Accept Friend", accept_friend_request_url(:user_name => friend_request.user_name), class: "btn btn-sm btn-primary" %>
		</li>
	</ul>
<% end %>
